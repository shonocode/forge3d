{
  "name": "forge3d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name forge3d --branch main --no-bundle"
  },
  "dependencies": {
    "@babylonjs/core": "^8.50.0",
    "@babylonjs/loaders": "^8.50.0",
    "@babylonjs/serializers": "^8.50.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.3.0",
    "vite-plugin-pwa": "^1.2.0",
    "wrangler": "^4.0.0"
  }
}
